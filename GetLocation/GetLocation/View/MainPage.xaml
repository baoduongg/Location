<?xml version="1.0" encoding="UTF-8"?>
<view:PageBase
    xmlns:view="clr-namespace:GetLocation.View"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="GetLocation.View.MainPage"
    IconImageSource="iconslist"
    Title="ListRecord">
    <Grid>
      <ListView
          x:Name="lv"
          ItemsSource="{Binding Locations}">
          <ListView.ItemTemplate>
              <DataTemplate>
                  <TextCell Text="{Binding Source}"
                            Detail="{Binding Destination}">
                  </TextCell>
              </DataTemplate>
          </ListView.ItemTemplate>
</ListView>
    </Grid>
</view:PageBase>
